---
interface Props {
  title: string;
  text: string;
}

const { title, text } = Astro.props;
---

<section class="page-hero reveal">
  <div class="container page-hero-inner">
    <p class="eyebrow">Au-dela du cadre</p>
    <h1>{title}</h1>
    <p>{text}</p>
  </div>
</section>
